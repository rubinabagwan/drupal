(function($, Drupal) {
   Drupal.behaviors.mytheme = {
   attach: function(context, settings) {
       console.log(123);
      // $('.page-header').css('background','#001');
   }
    };
}) (jQuery, Drupal);